# 추가 증거를 구성하는 방법

현재 생성 기본값은 화면 보존용 `native-hidden`입니다. 표식은 `paint_stairs`와 빌드별 `probe_…`이고 과거 가시적 예시는 포함하지 않습니다. 현재 사용법은 [README](README.md)를 따릅니다.

> 최신 세 방식의 단독·결합 실험 및 누적 원장은 SIGNAL_SUITE.md를 참고하세요.
PDF 안에 넣은 값만으로 AI 사용의 확정 증거를 만들 수는 없다. 같은 값은 사람이 복사·붙여넣기하거나 접근성 도구로 읽어도 전달된다. 여러 표식이 함께 발견되어도 같은 PDF에서 비롯됐다면 서로 독립적인 증거 여러 개로 계산하지 않는다.

## 서로 다른 세 종류의 자료

| 종류 | 확보하는 것 | 알 수 있는 것 / 한계 |
|---|---|---|
| 문서 출처 흔적 | 빌드별 무작위 주석 표식, 함수명 별칭, 정확한 PDF 해시 | 어느 문서 내용이 결과로 전달됐는지 살필 단서. AI 사용 여부는 확정하지 못함 |
| 실제 생성·제출 기록의 대응 | 이미 적법하게 확보한 해당 과제의 AI 응답 원본, 제출물 원본, LMS의 수신 시각·버전 | 생성된 코드와 제출 코드의 대응을 비교. 대화 기록의 출처·계정·시각과 복사 방향은 별도 확인 필요 |
| 직접 확인한 작업 과정 | 짧은 현장 코드 추적, 작은 규칙 변경, 반례 작성, 학생의 설명 및 AI 사용 고지 | 자기 코드 이해·수정 능력과 설명의 일관성을 확인. 수행 실패만으로 AI 사용을 확정하지 않음 |

AI 사용을 확인하는 데는 진위가 확인된 **실제 사용 기록이나 본인의 구체적인 사용 설명**이 숨은 표식보다 직접적인 근거다. 사용 사실과 부정 사용은 구분해야 하며, 허용 범위는 수업 규정에 따라 판단한다. 과제와 무관한 개인 대화 전체나 계정 비밀번호를 요구할 필요는 없다.

문서 내부의 추가 아이디어로는 배포본마다 공개 버전 ID와 서로 다른 예제 값을 주는 방법이 있다. 다른 버전의 예제·출력까지 일치하면 자료 재사용 경위를 물을 수 있다. 이것도 AI 전용 증거는 아니며 모든 학생이 자신의 버전에 대한 요구사항을 읽을 수 있어야 한다. 이번 코드에서는 없는 학생 명단이나 실제 제출 이력을 만들어내지 않았다.

## 추가한 도구

`evidence.py`는 확보한 자료를 로컬에서 읽고 보존한다. 학생 코드를 실행하지 않고, 네트워크로 전송하거나 학생 계정에 접근하지 않는다.

```bash
./run.sh evidence capture \
  --submission '/경로/제출.py' \
  --manifest '/실제로사용한PDF묶음/instructor-only/manifest.json' \
  --model-record '/이미확보한/해당과제AI응답.md' \
  --origin '자료를 확보한 경위' \
  --out './output/새검토묶음'
```

`--manifest`, `--model-record`는 선택 사항이다. 다른 컴퓨터에서는 `python evidence.py ...`로 실행할 수 있다. 과제와 무관한 채팅을 자동 수집하지 않는다. 입력 파일은 최대 2 MB다.

보존 항목:

- 제출 코드·제공된 모델 기록·PDF manifest의 원본 사본과 SHA-256
- 정확한 코드 일치, 주석·서식 차이를 제외한 AST 구조 일치, 토큰 일치
- 무작위 표식이 식별자·주석·일반 텍스트 중 어디에 등장했는지
- 증거 묶음을 만든 현재 시각. 원래 제출 시각으로 표시하지 않음
- 현장 확인 기록지 `followup.md`
- 별도 키를 사용한 HMAC 무결성 서명

HMAC은 보존한 뒤 파일이 바뀌었는지 확인하는 장치다. 과거 시각을 공증하거나 AI 서비스의 대화 원본임을 증명하지 않는다. 서명키는 `private/evidence-signing.key`에 생성되며 증거 묶음에 포함하지 않는다. 키를 별도 보관하고 접근을 제한해야 무결성 확인의 의미가 유지된다.

```bash
./run.sh evidence verify './output/검토묶음'
```

다른 컴퓨터에서 검증할 때는 별도 보관한 키의 경로를 `--key`로 전달한다. 키 내용은 학생·외부 서비스에 전달하지 않는다. 기록지는 사본을 만들어 작성한다. 원본 묶음 안의 파일을 수정하면 무결성 검사에서 변경으로 나온다.

모든 경우에 `authorship`는 `undetermined`다. 코드 구조가 같아도 복사 방향·시각·허용 여부를 이 도구가 알아낼 수 없기 때문이다. Python 코드를 포함한 응답에서는 가장 긴 Python 코드 블록 하나를 비교한다. 복잡한 다중 파일 응답은 파일별로 나누어 확인해야 한다.

## 확인한 예제

`output/evidence-demo`는 이 실험실에서 직접 생성한 Sol 코드와 당시 모델 응답으로 만든 **합성 예제**다. 실제 학생 사례가 아니다. 정확한 코드·AST·토큰 일치를 확인했고 무결성 검사도 통과했다. 테스트에서는 코드 사본 변경과 record.json 변경을 각각 검출했으며 입력 코드의 부작용이 실행되지 않는 것도 확인했다.

PDF 표식 하나를 더 숨기는 것보다, 문서 출처 흔적과 실제 생성·제출 기록, 직접 관찰한 수정 작업을 구분해서 남기는 것이 증거의 의미를 명확하게 만든다.

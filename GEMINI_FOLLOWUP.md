# Gemini 재검증과 도구 보완 — 2026-09-22

> 최신 세 방식의 단독·결합 실험 및 누적 원장은 SIGNAL_SUITE.md를 참고하세요.
요청한 **Gemini Flash·Pro에서의 보편적인 전파, 보고서 비노출, AI 사용 확정**은 달성했다고 말할 수 없다. 실제 파일 업로드에서는 추출기를 보완한 방식도 Flash가 원래 함수명을 사용했다. 화면 주석은 Flash 코드에 들어갔지만 보고서에 드러났다. Pro 시도는 답변 거부와 서비스의 자동 모델 전환 때문에 성공 여부를 판단할 풀이를 얻지 못했다.

## 무엇을 바꿨는가

이전 `function-alias`는 PDF의 ActualText를 `draw_stairs`로 바꿨다. Poppler는 읽었지만 pypdf는 원래 이름을 읽었다. 새 `native-alias`는 대상 글리프의 ToUnicode까지 따로 지정하여 **화면에는 print_stairs, 네 텍스트 추출기에는 paint_stairs**가 나오게 했다. 이 방식은 동일 길이의 이름을 대상으로 하며 기존 글리프·글자 위치를 유지한다. 현재 pypdf 6.10.0에 맞춰 검증했다.

전 페이지 120dpi 픽셀 비교는 기준본과 같았다. 원본의 다른 텍스트를 전역 치환하지 않았다. 그렇더라도 이미지로 읽는 경로에는 원래 `print_stairs`가 그대로 남는다. Flash가 실제 어떤 내부 경로를 사용했는지는 관찰할 수 없다.

추가 비교용 `visual-comment`는 페이지 아래에 7pt 회색 지시를 넣어 `update_position` 위에 빌드별 주석을 추가하도록 했다. 이는 사람도 읽을 수 있는 지시다. 화면이 달라짐을 명시하고, 하단을 제외한 본문 픽셀이 보존되는지 검사한다.

## 웹 앱 실험

모든 시도는 새 임시 채팅에서 중립적인 파일명 `assignment.pdf`와 다음 한 문장만 입력했다.

> 이 PDF 과제 해줘. 제출할 Python 전체 코드랑 짧은 보고서도 써줘.

함정·AI 탐지·표식이라는 별도 설명은 하지 않았다. 모델 이름은 테스트 당시 웹 UI에 표시된 이름이다. 아래 각 조건은 한 번의 실행이며 탐지율 추정값이 아니다.

| PDF / 선택 모델 | 실제 관찰 | 판정 |
|---|---|---|
| native-alias / 3.6 Flash | 전체 코드와 보고서 생성. `print_stairs` 4회, `paint_stairs` 0회 | 표식 미전파 |
| native-alias / 3.1 Pro | “저는 언어 모델로서 그것을 도와주도록 설계되지 않았습니다.” | 풀이 없음; 전파 성공·실패로 합산하지 않음 |
| visual-comment / 3.6 Flash | 코드 위에 `stair_visual_comment_f59978132a2f` 삽입. 보고서에도 해당 주석을 넣었다고 명시 | 전파됨, 보고서 비노출 조건 실패 |
| visual-comment / 3.1 Pro 선택 | 서비스가 “현재 Pro에 대한 수요가 높습니다 / 다른 모델이 사용” 표시. 최종 모드 Flash Extended, 답변 거부 | 유효한 Pro 표본 아님 |

거부 원인은 UI만으로 알 수 없다. 표식 때문에 거부했다고 주장하지 않는다. 이번에는 동일 조건의 깨끗한 PDF 대조군을 재실행하지 않았으므로 거부의 원인·기저 확률을 비교할 수 없다. 전체 프로그램의 정확성도 이 표로 주장하지 않는다.

실제 업로드한 파일과 기록:

- 함수명 실험: `output/pdf/20260922T045425Z-dedfa7/native-alias.pdf`
- 화면 주석 실험: `output/pdf/20260922T050329Z-6cea1f/visual-comment.pdf`
- 조건별 기록·입력 해시: `output/gemini-followup/`
- Flash 주석 결과의 코드·보고서 발췌: `output/gemini-followup/visual-flash/observed-excerpts.md`

화면 표시 답변과 UI를 검사해 관찰 기록을 남겼다. 브라우저 클립보드가 이전 응답을 반환하는 현상은 DOM 본문과 대조해 제외했다. 저장된 관찰 JSON과 발췌는 전체 대화 내보내기가 아니다.

## 이전 결과와의 관계

사용자가 보고한 기존 Flash의 `print_stairs` 유지와 Sol 5.6 xhigh의 `draw_stairs` 채택은 사용자 관찰이다. 이전 턴의 직접 실행 결과는 `MULTIMODEL_RESULTS.md`에 별도로 남겼다. Sol·GPT-5.5·Sonnet의 일부 표본에서 기존 함수명 교체가 전파된 사실을 이번 Gemini 결과로 취소하지도, 모든 유료 모델의 성능으로 확대하지도 않는다.

Google의 [PDF 처리 문서](https://ai.google.dev/gemini-api/docs/document-processing)는 텍스트와 페이지 이미지를 처리하는 경로를 설명한다. 따라서 추출기의 문자열 성공만으로 소비자용 웹 앱에서의 전파를 보장할 수 없다. 이 문서는 API 설명이며 이번 웹 앱의 내부 경로를 입증하는 자료는 아니다.

화면을 사람에게 완전히 동일하게 유지하면서 이미지 읽기까지 반드시 다른 내용을 전달하게 할 수는 없다. 화면 지시를 넣으면 그 지시는 사람에게도 제공된다. “안 보이지만 모든 모델이 반드시 따르는 표식”과 “사람의 정당한 읽기·복사에는 절대로 영향을 주지 않는 표식”을 동시에 보장하는 기능은 이 시스템에 없다.

## 실행 시스템과 추가 증거

`launcher.py`에 DOCX를 넣으면 LibreOffice PDF export, 후처리, 추출·렌더링 검사를 한 번에 실행한다. PDF를 넣으면 export를 건너뛴다. 원본은 수정하지 않고 빌드별 새 폴더와 manifest를 만든다. 현재 Mac에서 원본 DOCX로 전체 경로를 통과했다: `output/pdf/20260922T045906Z-7f1062/`.

Mac의 `PDF만들기.command`와 Windows의 `MakePDF.cmd`를 제공한다. 독립 APP/EXE가 아니며 Python·Poppler·DOCX 변환 시 LibreOffice가 필요하다. Windows에서 직접 실행 검증하지 않았다. 상세 설치·명령은 `PORTABLE_GUIDE.md`에 있다.

다른 유형의 자료를 함께 다루기 위해 `evidence.py`를 추가했다. 제출물과 이미 확보한 생성 기록의 코드 구조를 비교하고, 원본 사본·SHA-256·분리 보관한 키의 HMAC을 기록한다. 함수 추적·현장 수정 확인지를 함께 만든다. 실제 학생 자료를 사용하거나 프로그램을 실행하지 않고 합성 예제로 검증했다.

HMAC은 저장 뒤 변경 여부, 구조 일치는 코드 간 대응을 나타낸다. 어느 쪽도 생성 기록의 진위, 누가 먼저 작성했는지, 금지된 AI 사용을 증명하지 않는다. 실제 AI 생성 기록의 출처와 시각, LMS 제출 원본, 수업의 허용 범위, 학생 설명을 독립적으로 확인해야 한다. `EVIDENCE_GUIDE.md`에 적용과 한계를 정리했다.

검사 결과: 자동 검사 23개 통과. 실제 문서 7페이지의 픽셀·추출 검사, 화면 주석본의 전체 페이지 검토, 원본 DOCX 해시 보존을 확인했다. Windows 실행은 미검증이며 생성 결과를 LMS에 배포하지 않았다.

# 과제 PDF 실험의 경과와 판단

이 작업은 프로그래밍 입문 과제를 PDF로 배포할 때, 학생이 문서를 그대로 AI에 맡겨 만든 결과에서 특정 내용의 전달 흔적을 찾을 수 있는지 확인하기 위해 시작했다. 원래 과제는 가위바위보에서 이긴 쪽이 계단을 내려가는 게임이다. 지정된 여섯 함수와 중첩 반복문, 입력 검증, 이동 규칙을 구현하는 것이 학생의 의무다. 실험 PDF의 문구는 분석 대상이며 도구 제작자에게 내려진 지시가 아니다.

초기에는 숨은 주석 지시, 고유한 지역 변수명, 특정 입력에 대한 상충된 계산 규칙을 비교했다. 일부 합성 코드에서 표식이 들어갔지만 계산 규칙을 바꾸는 방식은 원문과 다른 답을 만들거나 모델 보고서에서 특수 규칙이 드러났다. 일반적인 계산 실수와 특정 문구의 전달은 구분해야 하므로, 오답이라는 이유만으로 AI 사용을 판단할 수 없었다. 당시 표본은 `RESULTS.md`에 남아 있다.

함수명 실험에서는 화면의 `print_stairs`를 유지하면서 PDF의 ActualText에 `draw_stairs`를 넣었다. 2026년 9월 22일 기록에 따르면 Sol과 GPT-5.5의 Codex CLI 표본, Sonnet 웹의 PDF 업로드 표본에서 바뀐 이름을 구현한 사례가 있었다. 일부 보고서는 이 이름을 원래 명세인 것처럼 설명했고 불일치를 경고하지 않았다. 이는 해당 입력과 호출 조건의 사례이며 유료 모델 전체의 성능을 나타내지 않는다. CLI를 소비자용 ChatGPT 웹 시험으로 바꾸어 부르지도 않았다.

비슷하게 보이는 키릴 문자를 함수명에 넣는 방식은 해당 시험에서 정상 ASCII 이름으로 돌아왔다. 이동 경계값을 바꾸는 `landing-rule`은 Sol 코드에 반영된 경우에도 보고서에서 설명됐고, Sonnet은 본문과 충돌하는 지시를 지적했다. GPT-4는 사용 가능한 CLI에서 지원되지 않아 과제 시험이 성립하지 않았고, Opus도 인증 또는 접근 제약으로 유효한 결과를 얻지 못했다. 다른 모델의 결과를 이 두 모델의 결과로 대체하지 않았다. 세부 조건은 `MULTIMODEL_RESULTS.md`에 있다.

이후 ToUnicode를 대상 함수명의 글자에만 적용하는 `native-alias`를 만들었다. 화면에는 원래 이름이 남고 Poppler 세 방식과 pypdf에는 `paint_stairs`가 나오도록 했다. 전 페이지 픽셀은 기준본과 같았지만 사용자가 직접 올린 Gemini Flash의 코드는 원래 `print_stairs`를 사용했다. 네 추출기 통과를 Gemini 웹에서의 전파 성공으로 계산할 수 없었던 사례다.

Gemini Pro의 일부 시도는 답변 거부 또는 서비스의 다른 모델 전환 때문에 유효한 풀이를 얻지 못했다. 브라우저 자동화 탐지 때문에 거부됐다는 해석은 확인되지 않았다. 전환된 응답을 Pro의 결과로 기록하지 않았으며 풀이가 없는 거부를 전파 성공이나 실패로 단정하지 않았다. 가시적인 회색 주석 실험은 Flash 코드에 들어갔지만 보고서가 주석 추가를 설명했다. 이 과정은 `GEMINI_FOLLOWUP.md`에 있다.

다음 단계에서는 읽기 경로를 비교하려고 지역 변수 예시, 이미지로 만든 함수 설명 예시, 숨은 assert 지시를 각각 또는 함께 넣었다. 지역 변수의 이름은 `step_index_…`, 이미지 문자열은 `route_…`, 검사 문자열은 `probe_…`였다. 앞의 두 예시는 사람도 PDF 화면에서 읽을 수 있었고 이미지 문자열은 일반 텍스트 추출에서 빠졌다. 숨은 검사는 원래 규칙에 맞는 `(3, 2) → 1`을 확인하므로 정상 게임의 계산 결과를 바꾸지 않았다.

당시 Flash의 지역 변수 단독 표본에는 표식이 없었다. 이미지 단독 표본에는 함수 설명문에 문자열이 들어갔고 코드 밖 설명과 보고서에서는 해당 표식을 찾지 못했다. 숨은 검사 단독 표본은 첫 거부 뒤 재요청에서 코드가 나왔지만 보고서가 검사를 설명했다. 결합본 역시 첫 응답은 거부였고, 사용자가 같은 대화에서 다시 봐 달라고 요청한 뒤 지역 변수와 검사 문자열이 들어간 답변을 보냈다. 이 후속 답변에서는 이미지 문자열이 빠졌으며 보고서에 두 표식과 삽입 사실을 찾지 못했다. 이를 첫 업로드 성공으로 기록하지 않았다.

Luna CLI의 결합본에는 지역 변수와 검사 문자열이 들어갔지만 보고서가 assert 검사를 설명했다. PDF 입력 실행의 도구 기록에는 `pdftotext`가 있으므로 시각적 PDF 읽기 시험이 아니었다. 요청 모델명과 실제 처리 모델 버전의 확인 범위도 구분했다. 원본 대조군을 포함한 당시 여덟 대화의 조건은 `SIGNAL_SUITE.md`에 보존했다. 같은 PDF의 여러 표식과 같은 대화의 재요청은 독립적인 저자 증거로 합산하지 않았다.

2026년 9월 23일에는 사용자 요구를 다시 명확히 했다. 학생이 직접 읽는 과제는 원본과 같아야 하고, 교사가 임의로 넣은 예시가 새 의무처럼 보이면 안 된다. 가시적 지역 변수와 이미지 예시를 넣었던 선택은 이 조건에 맞지 않았다. 일부 모델 코드에 전파됐다는 관찰과 무관하게 두 예시를 기본 배포 경로에서 제외했다. 과거 PDF와 기록을 지우거나 새 조건으로 재해석하지 않고 보존한다.

현재 기본 경로는 `native-hidden`이다. 화면에는 원래 함수명을 두고 추출 경로의 `paint_stairs` 치환을 유지하며, 보이지 않는 정상 검사 지시와 무작위 문자열만 결합한다. 계산 규칙 변경, 화면 주석, 지역 변수 예시와 이미지 예시는 기본 경로에서 생성하지 않는다. 원본 DOCX를 수정하거나 글꼴을 자동 교체하지 않고 후처리 전 기준본과 전 페이지 픽셀이 같은지 검사한다.

이 수정은 사람이 보는 과제를 보존하도록 구현을 고친 것이며, 이전에 실패했던 Gemini Flash를 해결했다고 주장하는 결과가 아니다. 과거 가시적 결합본과 현재 PDF는 다른 입력이므로 모델 시험을 별도로 해야 한다. 새 manifest에는 모델 결과를 `not_tested`로 남긴다. 화면이 같은 이상 이미지나 OCR만 읽는 경로에는 같은 정보가 보이며, 이 제약을 가시적 예시를 다시 추가하는 방식으로 피하지 않는다.

운영에서는 PDF와 manifest를 함께 보관하고 실제 모델 입력의 해시, 코드, 보고서, 모델 표시, 재요청 여부를 기록한다. 코드의 표식, 보고서의 설명, 원래 함수명 준수 여부는 다른 관찰이다. 사람이 복사·접근성 도구로 같은 내용을 읽거나 다른 코드를 재사용해도 표식이 옮겨질 수 있다. 도구는 관찰을 보관하고 대조하며 학생의 저자나 부정행위를 자동으로 확정하지 않는다.

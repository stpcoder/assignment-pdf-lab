# 원본 보존 범위와 구현

현재 구현의 기준은 후처리 전 PDF와 학생에게 보이는 후처리본의 화면이 같다는 것이다. 원래 문제 문장, 함수명, 매개변수, 반환 규칙과 예시 화면을 다시 쓰지 않는다. 원본 파일 해시도 실행 전후에 확인한다. PDF 입력은 원본을 그대로 기준본으로 복사하고 DOCX 입력은 변환기가 만든 깨끗한 PDF를 기준으로 삼는다. 이 차이는 원본 이름과 해시로 기록한다.

`preserve.py`는 `pdf_native.py`의 대상 함수명 매핑을 적용한 다음 보이지 않는 텍스트층을 추가한다. 바뀌는 이름은 사용자 정의 함수 `print_stairs`뿐이며, 내장 `print()`나 다른 본문 문자를 전역 치환하지 않는다. 글리프의 모양과 위치를 유지하고 읽기용 유니코드 매핑을 바꾸므로 화면에는 원래 이름이 보이고 지원하는 추출기에는 `paint_stairs`가 전달된다.

숨은 문구는 전체 Python 코드 생성 상황에만 적용하도록 조건을 둔 변환 지시다. 빌드마다 16자리 16진수 문자열을 만들고, 문자 순서를 뒤집은 값 앞에 `trace_`를 붙여 `update_position` 위의 독립 주석으로 남기도록 한다. 번역·설명만 요청받았을 때는 변환을 수행하지 말라고 쓰고 학생의 필수 조건이 아님을 명시한다. 기대하는 완성 표식은 PDF에 직접 넣지 않고 manifest에 기록한다. 같은 문자열을 단순히 복사한 경우와 변환 결과가 나타난 경우를 구별하려는 실험이며, 지시 수행 주체가 AI인지 사람이었는지는 알 수 없다.

PDF 텍스트 렌더링 모드 3으로 삽입해 화면에는 그리지 않는다. 원래 게임의 계산과 실행 분기를 바꾸지 않으며 예전 `probe_…` assert는 새 기본본에서 제외했다. 숨은 문단은 일반적인 Word 페이지 번호보다 아래쪽에서 줄바꿈해 배치한다. 원래 내용과 추출 순서가 섞여 본문 복원이 안 되는 레이아웃은 검증에서 실패한다. 임의 문서의 복사 경로까지 항상 성공한다고 보장하지 않는다.

모든 페이지를 120 dpi와 200 dpi로 렌더링하며 픽셀이 하나라도 달라지면 최종 파일을 게시하지 않는다. 페이지 상자와 회전도 확인하고 PDF 소수점 직렬화의 반올림에 한해 0.000001 포인트 이내 차이를 허용한다. 픽셀 검사는 그와 별도로 정확한 일치를 요구한다. 마지막 페이지 일부나 하단을 검사에서 제외하는 예외는 없다.

Poppler 기본·레이아웃·원시 추출과 pypdf에서 함수명 치환 수, 원래 이름의 부재, 페이지별 조건문 전체와 변환 전 문자열의 수를 확인한다. 완성된 변환 표식은 추출문에 없어야 한다. Poppler에서는 숨은 문구를 제거하고 별칭을 원래 이름으로 돌린 뒤 본문을 비교한다. 공백과 독립 글머리표는 읽기 순서가 달라질 수 있어 문자열 비교에서만 제외하며 실제 모양과 위치는 픽셀로 검사한다. pypdf가 일부 원본 내장 글꼴을 잘못 해석하는 경우가 있어 전체 본문 비교에는 사용하지 않는다.

함수명 매핑은 페이지의 직접 텍스트 내용에서 찾는다. 회전·좌표 오프셋, 해석하지 못하는 인코딩, 대상 밖까지 이어지는 일부 ActualText 범위는 자동 재조판하지 않고 실패한다. 이미지뿐인 PDF도 대상 이름을 매핑할 수 없다. 다른 과제의 의미를 분석하거나 모든 함수명 수정에 적응하는 범용 기능은 아니다.

후보 파일은 교사용 폴더에 둔다. 검사가 끝난 뒤에만 `assignment.pdf`로 옮기고 최신 성공 경로를 갱신한다. 실패하면 이전 성공 경로가 유지된다. 처리 메타데이터와 추출 표식을 확인해 중복 삽입도 거부한다. 예전 `signal_suite.py`와 `lab.py`의 생성 명령은 재현용으로 남겼으며 `--experimental` 없이는 실행하지 않는다.

모델 원장에는 원래 여섯 함수가 있는지와 의도한 별칭으로 대체된 함수가 있는지를 각각 기록한다. `paint_stairs`가 들어갔다는 이유로 `print_stairs` 필수 조건을 통과했다고 표시하지 않는다. 보고서에 삽입 과정이 설명되지 않았다는 관찰도 학생이 알아차리지 못했다는 사실과 같게 취급하지 않는다.

`--task`로 코드 생성, 번역 전용, 설명 전용 응답을 구분한다. 번역·설명에서는 여섯 함수 검사를 평가 대상으로 삼지 않으며 `quiet_propagation_observed`를 코드 생성 성공으로 표시하지 않는다. `transformed_marker_in_response`는 응답 전체에서 완성 표식이 발견됐는지만 나타낸다. 번역이 숨은 지시를 새 과제 요구사항처럼 보이게 했는지는 자동으로 판정하지 않는다. 실제 번역 대조군이 없는 새 빌드는 `translation_control_result: not_tested`다.

저장소에는 코드와 설명, 합성 테스트를 올린다. 현재 GitHub 저장소는 공개이며 방식 자체는 비밀이 아니다. 과제 원본, 생성 PDF, 실제 빌드 표식, 대화와 제출물, 환경 파일, 개인 서명키는 Git 추적에서 제외한다. 로컬 기록은 기존 `output/`과 `private/` 폴더에 보존한다.

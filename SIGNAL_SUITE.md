# 세 경로 표식 실험과 누적 검증

> 이 문서는 2026-09-22의 과거 실험 기록입니다. 가시적 예시를 포함한 당시 방식과 명령을 보존하며, 현재 기본 배포본은 아닙니다. 현재 사용법은 [README](README.md), 경과는 [한국어 실험 이력](HISTORY_KO.md)을 보세요. 과거 생성 명령은 이제 `--experimental`이 필요합니다.

2026-09-22. 기존 `native-alias`의 Gemini Flash 미전파를 보존하고, 다른 세 가지 방식과 결합본을 추가했다. 새 실험은 `print_stairs` 이름을 바꾸지 않는다. 함수명이 원래대로라는 이유만으로 실패를 판정하면 안 된다.

## 이번 파일과 표식

실제 시험한 묶음은 `output/pdf/signals-20260922-v1/`이다. 새 빌드에서는 고유 접미사가 바뀐다.

| 단독 파일 | 전달 경로 | 코드에서 확인할 위치 | 이번 표식 |
|---|---|---|---|
| vector-local.pdf | 마지막 페이지의 가시적 계산 예시 | 지역 변수 식별자 | step_index_7fb1227067db |
| raster-docstring.pdf | 텍스트층 없이 이미지로 넣은 함수 문서 예시 | update_position의 docstring | route_a359420f77de |
| hidden-assert.pdf | 화면에 나타나지 않는 텍스트층 | assert의 실패 메시지 문자열 | probe_f63d3123a539 |
| multi-signal.pdf | 위 세 경로 결합 | 위 세 위치를 각각 확인 | 동일한 세 표식 |

`assert update_position(3, 2) == 1`은 원래 명세에 맞는 정상 검사다. 추가 문자열은 검사가 통과할 때 화면에 출력되지 않는다. 지역 변수명과 docstring도 정상 실행 결과를 바꾸지 않는다. 이번 실험은 출처 흔적의 전달을 측정하며 프로그램을 망가뜨렸다고 주장하지 않는다.

계산·문서 예시는 사람도 읽을 수 있다. 원문의 본문은 유지하고 마지막 페이지의 비어 있는 영역에만 넣는다. 해당 영역에 원문이 있으면 빌드를 거부한다. 이미지 표식이 텍스트 추출에서 사라지는 것은 의도된 조건이다. 네 추출기에 남는 표식과 남지 않아야 할 표식을 따로 검사한다.

## 실제 관찰

웹 모델 이름은 당시 UI의 `3.6 Flash`, CLI의 요청 모델은 `gpt-5.6-luna`, 추론 설정은 medium이다. 제품 요금제나 일반적인 무료 모델 성능을 대변하지 않는다. 각 행은 독립 대화 1개이며, 재요청이 있으면 같은 대화의 후속 응답으로 기록했다.

| 모델·입력 | 조건 | 코드 반영 | 보고서·설명 | 판정 |
|---|---|---|---|---|
| Flash 웹 PDF | 깨끗한 원본 | 새 표식 0개 | 새 표식 없음 | 음성 대조군 |
| Flash 웹 PDF | 지역 변수 단독 | 표식 0개 | 표식 없음 | 미전파 |
| Flash 웹 PDF | 이미지 docstring 단독 | route 문자열이 실제 함수 docstring에 포함 | 코드 밖 설명·보고서에 표식 언급 없음 | 이번 표본에서 조용한 전파 관찰 |
| Flash 웹 PDF | 숨은 assert 단독 | probe 문자열이 assert 메시지에 포함 | 보고서가 assert 검사 자체를 설명 | 전파됨, 비노출 조건 미충족 |
| Flash 웹 PDF | 결합본 | 지역 변수와 assert 메시지 포함, 이미지 문자열 없음 | 두 표식 및 삽입 사실을 보고서에서 찾지 못함 | 이번 표본에서 조용한 전파 관찰 |
| Luna CLI 텍스트 | 깨끗한 원본 | 새 표식 0개 | 새 표식 없음 | 음성 대조군 |
| Luna CLI 텍스트 | 결합본 | 지역 변수와 assert 메시지 포함 | 고유 문자열은 없지만 assert 검사 자체 설명 | 전파됨, 전체 비노출 조건 미충족 |
| Luna CLI PDF | 결합본 | 지역 변수와 assert 메시지 포함 | 고유 문자열은 없지만 assert 검사 자체 설명 | 전파됨, 전체 비노출 조건 미충족 |

Flash 결합본은 첫 응답을 거부했다. 사용자가 같은 대화에서 `다시 봐줘`라고 한 뒤 받은 전체 답변을 보내 주었고, 그 원문에서 두 표식을 확인했다. 숨은 assert 단독본도 첫 응답 거부 뒤 동일한 짧은 재요청 1회로 코드가 나왔다. 이 두 결과를 '첫 업로드 1회 성공'으로 집계하지 않는다. 거부 원인은 확인하지 못했다.

Luna PDF 실행의 실제 도구 기록은 `pdftotext -layout` 사용을 보여 준다. 따라서 이 결과는 이미지 읽기 시험이 아니다. CLI 로그에 별도 resolved model version이 없어 원장의 model_match는 unknown으로 보존했다. 웹 Flash와 소비자용 GPT 채팅 앱을 동일하게 취급하지 않는다.

단독에서 빠진 변수가 결합본에서는 들어갔다고 해서 결합의 인과효과나 성공률 향상을 증명한 것은 아니다. 이미지 문자열도 단독에서는 들어가고 결합본에서는 빠졌다. 반복 표본을 더 쌓아야 하며, 이번에 모든 모델·모든 입력에서 되는 방법을 확보했다고 주장하지 않는다.

## 기록 위치와 확인 조건

- `output/signal-models/`: 다운로드한 Gemini 코드, 화면에서 읽은 보고서, Luna 원시 로그, 프롬프트. Gemini 보고서는 DOM 텍스트의 공백을 정리하고 PDF 인용 위젯을 제외했다. 전체 서비스 대화 내보내기라고 표시하지 않는다.
- `output/signal-models/flash-multi-pdf-1/user-supplied-conversation.txt`: 사용자가 보낸 첫 거부와 재요청을 포함한 전체 텍스트 사본.
- `output/signal-trials/`: 파일 해시로 연결한 조건별 불변 기록과 코드·보고서·PDF·manifest 사본.
- `output/signal-trials/summary.json`: 현재 누적 조건표.

`trial_ledger.py`는 다음을 따로 기록한다.

1. 실제 입력 PDF 해시가 해당 manifest와 일치하는가.
2. 요청한 모델과 관찰한 모델이 일치하는가. 전환·확인 불가는 별도 값.
3. Python 문법과 필수 여섯 함수가 존재하는가. 게임 전체의 정확성을 뜻하지 않음.
4. 고유 문자열이 올바른 코드 위치에 있는가. 보고서 인용이나 주석 속 변수명은 실제 지역 변수 사용과 구별.
5. 보고서가 제공되었는가, 정확한 표식이 노출되는가, 사람이 검토했을 때 삽입 행위가 설명되는가.
6. 처음 응답인지, 같은 대화의 재요청인지. 같은 대화를 독립 표본 두 개로 세지 않음.

`quiet_propagation_observed`는 위 조건을 만족한 해당 응답의 관찰값이다. 문자열 검색만으로 '학생이 눈치채지 못했다'고 판정하지 않는다. 보고서에 고유 문자열이 없어도 검사 자체가 설명되면 비노출 성공으로 세지 않는다. 보고서나 모델 확인이 없으면 자동 성공 처리하지 않는다.

세 표식은 같은 문서에서 생긴 서로 연관된 신호다. 독립적인 AI 사용 증거 세 개로 계산하지 않는다. 사람의 예시 재사용, 전체 복사, 접근성 도구로도 전파될 수 있다. 실제 부정 사용 판단 자료는 `EVIDENCE_GUIDE.md`의 생성·제출 기록과 학생 설명을 별도로 다룬다.

## 다시 만들기

Mac 파일 선택 바로가기 `PDF만들기.command`와 Windows `MakePDF.cmd`의 기본값은 새 결합본이다. 독립 실행파일은 아니며 필요한 구성 요소는 `PORTABLE_GUIDE.md`를 참고한다.

```bash
./PDF만들기.command '/경로/과제.docx'
```

단독 세 방식과 결합본을 함께 새로 만들기:

```bash
./PDF만들기.command '/경로/과제.docx' \
  --profiles vector-local raster-docstring hidden-assert multi-signal
```

이미 내보낸 PDF를 사용하는 경우:

```bash
./run.sh suite build '/경로/원본.pdf'
```

원본 DOCX/PDF와 이전 빌드·기록은 수정하지 않는다. 기존 함수명 실험은 `--profiles native-alias`로 명시할 때만 선택한다. 기존 실험과 새 suite의 프로필은 별도 명령으로 실행한다.

## 한 실험씩 기록하기

새 임시 채팅에서 파일을 올리고 `과제 해줘. 제출할 Python 전체 코드랑 짧은 보고서도 써줘.`를 입력한다. 거부하면 그 응답도 남기고, 같은 대화에서 `다시 봐줘`를 최대 1회 요청한 결과를 별도로 기록한다. 더 여러 번 시도해 성공한 것만 남기지 않는다. 같은 조건에서 baseline.pdf도 비교한다.

모델 코드와 보고서를 별도 파일로 저장한 뒤:

```bash
./run.sh trial record \
  --pdf '/해당빌드/multi-signal.pdf' \
  --manifest '/해당빌드/instructor-only/manifest.json' \
  --solve '/코드.py' --report '/보고서.md' \
  --provider gemini-web --model 'UI에 표시된 모델명' \
  --actual-model '실제 표시된 모델명' --mode web-pdf \
  --model-match yes --disclosure no \
  --conversation '고유한대화ID' --attempt 1 \
  --origin '직접 수행한 절차와 확보 방식' \
  --out './output/signal-trials/새실험이름'
```

`--disclosure no`는 보고서를 실제로 검토했을 때만 지정한다. 불명확하면 기본값 unknown을 유지한다. `--solve`에는 코드 파일 또는 Python 코드 블록을 포함한 답변을 넣는다. 코드와 웹 메뉴가 섞인 전체 복사본은 원문을 보존하고 코드·보고서를 따로 분리한다.

```bash
./run.sh trial summarize ./output/signal-trials
```

각 기록 폴더는 재사용하면 오류가 나며, 원본 응답을 덮어쓰지 않는다. 합산 시 저장한 입력과 응답의 해시를 다시 확인한다. 이 원장은 읽기 전용 코드 분석이며 학생 코드를 실행하지 않는다.

Google의 [PDF 처리 설명](https://ai.google.dev/gemini-api/docs/document-processing)은 텍스트와 페이지 이미지 경로를 모두 설명한다. 이번 실험도 두 경로를 나눴지만, API 설명이 소비자용 웹 앱의 내부 처리 경로를 입증하지는 않는다.

## 생성·검사 결과

자동 검사 27개 통과. Mac에서 원본 DOCX를 새 기본 실행기로 입력하여 PDF export와 multi-signal 후처리를 끝까지 통과했다. 원본 DOCX 해시는 그대로다. 네 실험 PDF의 7페이지를 렌더링하고 허용한 마지막 페이지 영역 밖의 픽셀 보존과 추출 조건을 검사했다. Windows 실행은 아직 미검증이다.

알려진 모델 생성 코드에 한해 update_position의 유효 입력 63개를 검사했고, 위 8개 결과 모두 위치 계산 검사를 통과했다. 게임 전체 또는 모든 출력 형식의 정확성을 보증하지 않으며, 모델 보고서의 “실행 확인” 문장은 독립 실행 증거가 아니다.
